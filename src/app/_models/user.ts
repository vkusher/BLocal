export class User {
    UserId: string;
    UserName: string;
    Password: string;
    FirstName: string;
    LastName: string;    
    FireBaseId: string;  
    PhoneNumber: string;  
    isOwner: Boolean;
    IsAdmin: Boolean;
    IsEmailApproved: Boolean;
}